{
  "name": "@cloudflare/artifact-fs-sandbox-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal Cloudflare Sandbox SDK example that mounts a Git repo with ArtifactFS.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/sandbox": "0.8.1",
    "@cloudflare/workers-types": "^4.20260320.0",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.103.0"
  }
}
